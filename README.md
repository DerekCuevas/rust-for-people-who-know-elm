# Rust For People Who Know Elm

## Setup / Tooling

`cargo` https://doc.rust-lang.org/cargo/getting-started/installation.html

- `cargo init` - `elm init`
- `cargo build` - `elm make`
- `cargo fmt` - `elm-format`
